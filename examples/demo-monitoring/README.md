# Demo monitoring

This end-to-end demo uses 80 entirely synthetic records. It intentionally contains positive matches, exclusions, expired items, low-score items, and duplicate pairs.

Use **Import and run Demo** in the desktop app. Relative timestamps are rebased at import time so the workflow remains useful in future releases.
